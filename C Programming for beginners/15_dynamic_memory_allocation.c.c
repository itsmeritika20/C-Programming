#include<stdio.h>

int main(){
    int a; //declaration statements and memory allocation depicts during compilation(compile time)=SMA(Static Memory Allocation)
    float b; //SMA
    int x[5]; //SMA
    return 0;
}